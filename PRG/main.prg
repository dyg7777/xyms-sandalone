#DEFINE HKEY_CLASSES_ROOT 2147483648 && 0x80000000，或用负数: -2147483648
#DEFINE HKEY_CURRENT_USER 2147483649 && 0x80000001，-2147483647
HKEY_LOCAL_MACHINE=2147483650 && 0x80000002，-2147483646
#DEFINE HKEY_USERS 2147483651 && 0x80000003，-2147483645
#DEFINE HKEY_PERFORMANCE_DATA 2147483652 && 0x80000004，-2147483644
#DEFINE HKEY_CURRENT_CONFIG 2147483653 && 0x80000005，-2147483643
#DEFINE HKEY_DYN_DATA 2147483654 && 0x80000006，-2147483642
SET DATE TO LONG
SET CENTURY ON
SET NOTIFY OFF
SET TALK OFF
*!*	IF FCREATE("d:\ss.dll")<=0
*!*		=MESSAGEBOX("程序禁止多开，以免出现未知错误！",0,"^_^^_^")
*!*		QUIT
*!*	ENDIF
PUBLIC QQQ,BGPCI,TB_1,DW_1,SWDJ_1,SQRQ_1,SQM_1,BBH_1,ZYM_1,XYINI,PATHH,GETREG,REG0,REG1,CKEY1,DBDPATH,PRGPATH,hqrmc
PATHH=ALLTRIM(SYS(5)+SYS(2003))
DBDPATH=PATHH+'\dbd\'
PRGPATH=PATHH+'\prg\'
SET LIBRARY TO MYFLL.FLL
OPEN DATABASE DBDPATH+'ckdbd\ckdb.dbc'&&出库数据库
OPEN DATABASE DBDPATH+'zmdbd\zmdb.dbc'&&账目管理数据
OPEN DATABASE DBDPATH+'djdbd\djdb.dbc'&&入库数据库
SET DATABASE TO DJDB
USE REGDB
STORE ALLTRIM(REGDB.DWMC) TO DW_1
STORE ALLTRIM(REGDB.SWDJH) TO SWDJ_1
STORE REGDB.SQSJ TO SQRQ_1
STORE ALLTRIM(REGDB.SQM) TO SQM_1
STORE ALLTRIM(REGDB.BBH) TO BBH_1
STORE ALLTRIM(REGDB.ZYM) TO ZYM_1
USE
MODI WINDOW SCREEN  NOCLOSE NOFLOAT NOGROW TITLE "鑫奕软件--粮食企业进销存管理系统(单机版)"+SPACE(5)+"登录时间："+DTOC(DATETIME())+TIME()
ZOOM WIND SCREEN MAX
XYINI=PATHH+'\xy.ini'
QQQ=INIREAD('主窗口背景图片',"default",'窗口背景',XYINI)
IF !FILE(QQQ)
	QQQ=PATHH+'\media\背景图片\psb.jpg'
ENDIF
BGPCI=INIREAD('子窗口背景图片',"default",'窗口背景',XYINI)
IF !FILE(BGPCI)
	BGPCI=PATHH+'\media\背景图片\风景04.jpg'
ENDIF
TB=INIREAD('窗口图标',"default",'窗口背景',XYINI)
IF !FILE(TB)
	TB=PATHH+'\media\tb.ico'
ENDIF
_SCREEN.ADDOBJECT("IMG","Image")
_SCREEN.LOCKSCREEN=.F.
_SCREEN.IMG.TOP=0
_SCREEN.IMG.LEFT=0
_SCREEN.IMG.WIDTH=_SCREEN.WIDTH
_SCREEN.IMG.HEIGHT=_SCREEN.HEIGHT
_SCREEN.IMG.VISIBLE=.T.
SETDESKTOP=2
_SCREEN.IMG.STRETCH=SETDESKTOP     &&SetDesktop:0   ,1-居中     2-拉伸
_SCREEN.IMG.PICTURE=QQQ
_SCREEN.ICON=TB
SELECT COUNT(*) INTO ARRAY A1 FROM YHDBF
SELECT * FROM hqr into CURSOR hqrtmp 
STORE ALLTRIM(hqrtmp.hqrname) TO hqrmc
DO WORKBAR
IF SQRQ_1<DATE() THEN
	DO PATHH+'\menu\regmenu.MPR'
ELSE
	IF A1>0 THEN
		TI=MESSAGEBOX('当前系统日期为：'+DTOC(DATE()),4+16,'系统日期提醒')
		IF TI=6
			DO FORM PATHH+"\SCX\LOGIN"
		ELSE
			DO FORM PATHH+"\SCX\settime"
		ENDIF
	ELSE
		DO PATHH+"\menu\mainm.MPR"
	ENDIF
ENDIF
READ EVEN

